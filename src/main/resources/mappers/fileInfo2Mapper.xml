<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fileInfo2">
    <!--    첨부된 파일 정보 보기-->
    <select id="fileInfoDetail" resultType="kr.co.haebop.domain.FileInfo2">
        select * from fileInfo2 where seq=#{seq}
    </select>


    <update id="fileboardEdit">
        update fileInfo2 set saveFolder=#{saveFolder}, originFile=#{originFile}, saveFile=#{saveFile} where seq=#{seq}
    </update>


    <!-- 자료실 번호 별 파일 리스트 가져오기 -->
    <select id="fileInfoList" resultType="kr.co.haebop.domain.FileInfo2">
        select * from fileInfo2
        where seq = #{seq}
    </select>

    <select id="fileInfoList2" resultType="kr.co.haebop.domain.FileInfo2">
        select * from fileInfo2
    </select>

    <insert id="fileRegister" parameterType="fileInfo2">
        insert into fileinfo2 (seq, savefolder, originfile, savefile)
        values
        <foreach collection="fileInfos" item="fileinfo2" separator=" , ">
            (#{seq}, #{fileinfo2.saveFolder}, #{fileinfo2.originFile}, #{fileinfo2.saveFile})
        </foreach>
    </insert>




    <!-- 파일 삭제 -->
    <delete id="fileInfoDelete">
        delete from fileInfo2
        where seq = #{seq}
    </delete>



    <!-- 파일 추가 -->
    <insert id="fileInfoInsert">
        insert into fileInfo2(seq, savefolder, originfile, savefile)
        values
        <foreach collection="fileInfos" item="fileInfo" separator=" , ">
            (#{seq}, #{fileInfo.saveFolder}, #{fileInfo.originFile}, #{fileInfo.saveFile})
        </foreach>
    </insert>


</mapper>